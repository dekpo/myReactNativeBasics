import React from 'react';
import { Text } from "react-native";

// un composant React fonctionnel
const FunctionComponent = () => {
    // varialble utilisée dans le code JSX
    const name = "CDA";
    return <Text>My first function component session {name}</Text>;
  }

export default FunctionComponent